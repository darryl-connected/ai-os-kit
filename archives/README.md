# Archives

Old stuff. Don't delete — move here.

Naming: `archives/{YYYY-MM-DD-HHMM}/{filename}` — full snapshot with timestamp, never overwrite.

Quarterly cleanup is the suggested cadence (see `EXPANSIONS.md`).